/**
 * /servers/new — Server creation wizard page.
 * Renders the 7-step ServerCreationWizard component.
 */
import { ServerCreationWizard } from "@/components/wizard/ServerCreationWizard";

export default function NewServerPage() {
  return <ServerCreationWizard />;
}
